# fhir-jp-validator
Local HL7 FHIR JP Core / JP-CLINS validation stack — HL7 fhirserver (with LOINC/SNOMED import patches) + HAPI Validator cluster + JP terminology setup. Rosetta 2 optimized for Apple Silicon.
